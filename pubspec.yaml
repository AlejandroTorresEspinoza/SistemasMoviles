name: untitled
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  quickalert: ^1.0.2
  sqflite: ^2.0.0
  path_provider: ^2.0.2
  crypto: ^3.0.1
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/portada.jpg
    - assets/portada.jpg

  fonts:
    - family: LeagueGothic
      fonts:
        - asset: assets/fonts/LeagueGothic.ttf
